<div class="habbo-card">
    <h2 class="text-2xl font-bold text-habbo-blue-dark mb-4 flex items-center gap-2">
        🔥 {{ __('Popular Help Topics') }}
    </h2>
    <div class="grid gap-3 md:grid-cols-2">
        <button class="habbo-btn-secondary text-left flex items-center gap-3 hover:scale-105 transition-transform">
            <span class="text-2xl">🔑</span>
            <div>
                <div class="font-bold">{{ __('Account & Login Issues') }}</div>
                <div class="text-xs text-gray-600">{{ __('Password resets, account security') }}</div>
            </div>
        </button>
        <button class="habbo-btn-secondary text-left flex items-center gap-3 hover:scale-105 transition-transform">
            <span class="text-2xl">🏗️</span>
            <div>
                <div class="font-bold">{{ __('Building & Decorating') }}</div>
                <div class="text-xs text-gray-600">{{ __('Room design tips and tricks') }}</div>
            </div>
        </button>
        <button class="habbo-btn-secondary text-left flex items-center gap-3 hover:scale-105 transition-transform">
            <span class="text-2xl">💰</span>
            <div>
                <div class="font-bold">{{ __('Credits & Trading') }}</div>
                <div class="text-xs text-gray-600">{{ __('How to earn and trade safely') }}</div>
            </div>
        </button>
        <button class="habbo-btn-secondary text-left flex items-center gap-3 hover:scale-105 transition-transform">
            <span class="text-2xl">🚫</span>
            <div>
                <div class="font-bold">{{ __('Report Issues') }}</div>
                <div class="text-xs text-gray-600">{{ __('How to report users or bugs') }}</div>
            </div>
        </button>
    </div>
</div>
